pipeline {
  triggers {
    githubPush()
  }
  agent {
    label 'net-core'
  }
  options {
    disableConcurrentBuilds()
    buildDiscarder (logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
  }
  stages {
    agent {
     label 'net-core'
          }
    stage("First step") {
      steps {
        sh 'hostname'
        sh 'pwd'
      }
    }
  }
}         
